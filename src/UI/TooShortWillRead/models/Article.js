class Article {
    constructor(id, header, text, imageLink) {
        this.id = id;
        this.header = header;
        this.text = text;
        this.imageLink = imageLink;
    }
}

export default Article;